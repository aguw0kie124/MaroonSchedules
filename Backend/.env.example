APP_ENV=production
CLERK_JWKS_URL=https://your-clerk-domain/.well-known/jwks.json
API_KEY=replace-with-your-app-api-key
CORS_ALLOW_ORIGINS=http://localhost:8081,http://127.0.0.1:8081,https://maroonlife.app
CLERK_JWT_AUDIENCE=
CLERK_ISSUER=

# Existing backend configuration
CLERK_SECRET_KEY=
DB_HOST=
DB_NAME=
DB_USER=
DB_PASS=
# Redis cache provider
# Works with standard Redis URLs, including Upstash's TCP Redis URL.
# Do not use the Upstash REST URL here; use the redis:// or rediss:// connection string.
REDIS_URL=
UPSTASH_REDIS_URL=

# --- RevAI assistant ---
# LLM provider for RevAI. If NVIDIA_API_KEY is set, RevAI uses NVIDIA NIM;
# otherwise it uses OpenRouter (OPENROUTER_API_KEY, shared with event classification).
NVIDIA_API_KEY=
NVIDIA_ASSISTANT_MODEL=nvidia/nemotron-3-ultra-550b-a55b
# Reasoning is much slower; keep off for a snappy assistant. Set true to enable.
NVIDIA_ENABLE_THINKING=false
# Fast lane (simple/greeting questions). Defaults to NVIDIA_ASSISTANT_MODEL; set a
# smaller model (e.g. nvidia/nemotron-3-super-120b-a12b) for snappier simple replies.
NVIDIA_FAST_MODEL=
# Agent lane (tool-calling). Ultra is too slow per call in a tool loop, so this
# defaults to Nemotron Super. Override if needed.
NVIDIA_AGENT_MODEL=
NVIDIA_AGENT_TIMEOUT=18
NVIDIA_AGENT_REQUEST_LIMIT=4
OPENROUTER_API_KEY=
# Optional: override RevAI's OpenRouter models (comma-separated). Defaults to DeepSeek v3.
OPENROUTER_ASSISTANT_MODELS=
# Optional: reliable web search for RevAI (free tier at tavily.com). Without it,
# keyless DuckDuckGo is used (entity results only).
TAVILY_API_KEY=
